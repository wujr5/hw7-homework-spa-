extends layout

block content
	div.container
		div.row
			div.col-sm-6.col-md-4.col-md-offset-4
				h1.text-center.login-title Sign in to Myhomework
				div.account-wall
					img(class='profile-img', src='/images/photo.png')
					form(class='form-signin', action='/login', method='POST')
						input(type='text', name='username' class='form-control', placeholder='Username',required, autofocus)
						input(type='password', name='password' class='form-control', placeholder='Password', required)
						div.role
							input(type='radio' name='role' value='student' id='student-role' checked)
							label(for='student-role') Student
							input(type='radio' name='role' value='teacher' id='teacher-role')
							label(for='teacher-role') Teacher
						button(class='btn btn-lg btn-primary btn-block', type='submit') Sign in
						span.clearfix
				a(href='/signup', class='text-center new-account') Create an account
				#message
				if message
					h1.text-center.error-message #{message}